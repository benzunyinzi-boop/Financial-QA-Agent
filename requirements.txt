# ==============================================
# AI RAG 智能体项目 依赖清单
# 克隆项目后执行：pip install -r requirements.txt
# ==============================================
#
# 版本策略：使用宽松范围约束，让 pip 自动解析兼容版本组合
# 原因：langchain 生态版本耦合严重，硬编码版本容易冲突

# 核心大模型框架（保持最新主版本兼容）
langchain>=0.3.27,<0.4.0
langchain-core>=0.3.78,<0.4.0
langchain-community>=0.3.27,<0.4.0
langchain-openai>=0.3.0,<0.4.0

# 向量数据库与文档处理
langchain-chroma>=0.1.4,<0.2.0
chromadb>=0.5.15,<0.7.0
langchain-text-splitters>=0.3.0,<0.4.0

# 智能体工作流（LangGraph + 持久化 checkpointer）
langgraph>=0.2.50,<0.3.0
langgraph-checkpoint>=2.0.0,<3.0.0
langgraph-checkpoint-sqlite>=2.0.0,<3.0.0
aiosqlite>=0.20.0

# Web 后端框架
fastapi>=0.115.0,<1.0.0
uvicorn>=0.30.0,<1.0.0
python-multipart>=0.0.20
aiofiles>=24.0.0
sqlalchemy>=2.0.30,<3.0.0

# 配置和工具
python-dotenv>=1.0.0
pyyaml>=6.0

# 文件解析
pypdf>=5.0.0
pymupdf>=1.24.0             # 替换 PyPDFLoader，更好地处理复杂 PDF（表格、多栏、页眉页脚）
python-pptx>=1.0.0          # PPT 解析（销售培训资料、产品讲解 deck）

# Hybrid Search 依赖（BM25 关键词检索 + 中文分词）
rank_bm25>=0.2.2
jieba>=0.42.1

# 模型服务（阿里通义千问/ dashscope）
dashscope>=1.20.0

# 旧版前端（可选保留）
streamlit>=1.40.0
